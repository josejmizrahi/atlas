# Supabase — proyecto local (supabase start) o remoto
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# SOLO para tooling local de desarrollo (seed, tests). Nunca en código de la app.
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic (Fase 3 — scribe). Vive en Edge Functions / worker, nunca en el cliente.
ANTHROPIC_API_KEY=
